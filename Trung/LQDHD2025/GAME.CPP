// Bai 3 @ assets/DE THI HSG 11 NAM 2024-2025 CAP TRUONG.docx
#include <fstream>
#include <vector>
using namespace std;
int main() {
    size_t n, k;
    ifstream in("GAME.INP");
    ofstream out("GAME.OUT");
    in >> n >> k;
    
    in.close();
    out.close();
}
